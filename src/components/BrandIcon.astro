---
import type { SimpleIcon } from "simple-icons";

interface Props {
  icon: SimpleIcon;
  size?: number;
  color?: string;
}
const { icon, size = 24, color = "currentColor" } = Astro.props;
---

<svg
  role="img"
  fill={color ?? `#${icon.hex}`}
  width={size}
  height={size}
  viewBox="0 0 24 24"
>
  <title>{icon.title}</title>
  <path d={icon.path}></path>
</svg>
